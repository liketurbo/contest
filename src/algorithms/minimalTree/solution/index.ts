import BinarySearchTree from "../../../dataTypes/BinarySearchTree/solution"

const minimalTree = (arr: number[]) => {
  const tree = BinarySearchTree.fromArray(arr)

  return tree
}

export default minimalTree
